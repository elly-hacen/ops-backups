name: Release APK

on:
  push:
    tags:
      - 'v*'

env:
  APP_NAME: ops-backups

jobs:
  release:
    name: Build & Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build Release APK
        run: ./gradlew assembleRelease
        env:
          TERMUX_TASKER_APP__BUILD__APP_VERSION_NAME: ${{ steps.version.outputs.VERSION }}

      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/release-key.jks
        
      - name: Sign APK
        run: |
          # Find the unsigned APK
          UNSIGNED_APK=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
          
          # Sign with jarsigner
          jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
            -keystore app/release-key.jks \
            -storepass "${{ secrets.KEY_STORE_PASSWORD }}" \
            -keypass "${{ secrets.KEY_PASSWORD }}" \
            "$UNSIGNED_APK" \
            "${{ secrets.KEY_ALIAS }}"
          
          # Verify
          jarsigner -verify "$UNSIGNED_APK"
          
          # Zipalign
          ${ANDROID_HOME}/build-tools/34.0.0/zipalign -v 4 \
            "$UNSIGNED_APK" \
            "app/build/outputs/apk/release/${APP_NAME}_${{ steps.version.outputs.VERSION }}.apk"
          
          # Remove unsigned
          rm "$UNSIGNED_APK"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: app/build/outputs/apk/release/${{ env.APP_NAME }}_*.apk
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-beta') || contains(github.ref, '-alpha') }}
          name: Release ${{ steps.version.outputs.VERSION }}
          body: |
            ## What's Changed
            
            Auto-generated release for ${{ steps.version.outputs.VERSION }}
            
            ### Installation
            1. Download the APK below
            2. Install on your Android device
            3. Grant necessary permissions
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

